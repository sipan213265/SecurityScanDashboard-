# Python requirements for document generation scripts (thesis & presentation)
# Main application is ASP.NET Core — .NET dependencies are managed via NuGet
# Run: pip install -r requirements.txt

python-docx>=1.1.2
python-pptx>=1.0.2
